// ==== НАЛАШТУВАННЯ ДОСТУПУ / МОНЕТИЗАЦІЇ ====
// Сюди впиши свій Telegram user id (число), щоб мати повний доступ до всіх тем,
// поки решта користувачів бачить лише безкоштовні теми + заглушку "розблокувати за Stars".
//
// Як дізнатись свій id: напиши боту @userinfobot в Telegram — він надішле твій числовий id.

const CONFIG = {
  // Приклад: [123456789]
  OWNER_TELEGRAM_IDS: [793682367],

  // Скільки перших тем безкоштовні для всіх (решта — "преміум", розблоковується Stars).
  // Для власника (OWNER_TELEGRAM_IDS) завжди відкрито все, незалежно від цього числа.
  FREE_TOPICS_COUNT: 3,

  // Монетизація увімкнена — оплата йде кнопкою прямо в застосунку (Telegram Stars),
  // без жодної команди боту. Реальний преміум-статус фронтенд перевіряє через бекенд
  // (BACKEND_URL нижче), а не локально.
  MONETIZATION_ENABLED: true,

  // Адреса бот-бекенду (bot-backend), який зберігає, хто оплатив преміум,
  // і видає посилання на оплату Stars. Онови, якщо адреса сервісу на Render зміниться.
  BACKEND_URL: "https://history-of-ukraine.onrender.com",

  // Тарифи для показу на кнопках — лише для відображення (текст/ціна на екрані).
  // Реальна сума, яку спишуть, завжди визначається на бекенді (bot-backend/src/pricing.js) —
  // якщо колись розійдуться в числах, спишеться саме бекендове значення, тож онови тут теж,
  // якщо міняєш ціни там.
  PRICING_TIERS: [
    { id: "month", title: "Місяць", stars: 50, hint: "30 днів повного доступу" },
    { id: "year", title: "Рік", stars: 250, hint: "365 днів повного доступу" },
    { id: "lifetime", title: "Назавжди", stars: 400, hint: "Без обмежень у часі" },
  ],

  // Назва застосунку, що показується у шапці.
  APP_NAME: "Історія України · Тренажер НМТ",
};

if (typeof module !== "undefined") { module.exports = CONFIG; }
